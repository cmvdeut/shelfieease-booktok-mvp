# UITGESCHAKELD (mei 2026) — gebruik Social Hub (docs/SOCIAL-HUB.md) of handmatig posten.
# Zie docs/MANUAL-SOCIAL-POSTING.md en scheduled-posts.json als planningskalender.
#
# Deze workflow draait niet meer op een schema. Alleen nog startbaar via GitHub UI
# en doet dan niets (if: false).

name: Publish Scheduled Social Media Posts (disabled)

on:
  workflow_dispatch:

concurrency:
  group: publish-scheduled
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  publish:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Automatisch posten is uitgeschakeld."
          echo "Gebruik docs/SOCIAL-HUB.md (lokaal) of scheduled-posts.json handmatig."
          exit 0
